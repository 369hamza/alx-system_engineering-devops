#!/usr/bin/env bash

# Bash script that displays listening ports

# Display a comment describing what the script does
echo "This script displays listening ports"

# No output is expected here, so we leave this section empty
